import type { Metadata } from 'next';import { Header } from '../ui/header';import { Footer } from '../ui/footer';import { FaqList } from '../ui/faq-list';import { WhatsApp } from '../ui/whatsapp';import { getFaqs } from '../lib/content';
export const metadata:Metadata={title:'Preguntas frecuentes',description:'Respuestas sobre reservas, pagos, seguridad, cancelaciones y tours de Bongo Outdoors.'};
export const dynamic='force-dynamic';export default async function Faqs(){const rows=await getFaqs() as Array<{question:string;answer:string}>;return <main><Header/><section className="page-hero page-hero--cream"><div className="shell"><p className="eyebrow"><span/> Centro de ayuda</p><h1>Preguntas frecuentes.</h1><p>Todo lo que necesitas saber antes de vivir la aventura.</p></div></section><section className="section shell narrow"><FaqList custom={rows.map(r=>[r.question,r.answer])}/></section><section className="contact-banner"><div className="shell"><div><p className="eyebrow eyebrow--light"><span/> ¿No aparece tu pregunta?</p><h2>Conversemos.</h2></div><a className="button button--primary" href="https://wa.me/50764467276" target="_blank">Escribir por WhatsApp</a></div></section><Footer/><WhatsApp/></main>}
