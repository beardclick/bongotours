'use client';
import { useEffect,useRef,useState } from 'react';
type Item={label:string;href:string};
export function SectionNav({items}:{items:Item[]}){const [active,setActive]=useState(items[0]?.href??'');const nav=useRef<HTMLDivElement>(null);useEffect(()=>{const targets=items.map(item=>document.querySelector(item.href)).filter((node):node is Element=>Boolean(node));if(!targets.length)return;const update=()=>{const marker=window.scrollY+150;let current=items[0]?.href??'';for(const item of items){const element=document.querySelector(item.href) as HTMLElement|null;if(element&&element.offsetTop<=marker)current=item.href;}setActive(current)};update();window.addEventListener('scroll',update,{passive:true});return()=>window.removeEventListener('scroll',update)},[items]);useEffect(()=>{const link=nav.current?.querySelector(`[href="${active}"]`) as HTMLElement|null;link?.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'})},[active]);return <nav className="mobile-section-nav" aria-label="Secciones de esta página"><div ref={nav}>{items.map(item=><a className={active===item.href?'active':''} aria-current={active===item.href?'location':undefined} key={item.href} href={item.href} onClick={()=>setActive(item.href)}>{item.label}</a>)}</div></nav>}
