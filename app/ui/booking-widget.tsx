'use client';
import { FormEvent,useMemo,useState } from 'react';
type Season={name:string;start:string;end:string;personPrice?:number;groupPrice?:number};
export function BookingWidget({tourSlug,tourName,personPrice,groupPrice,seasonPrices}:{tourSlug:string;tourName:string;personPrice:number;groupPrice?:number;seasonPrices?:string}){const [mode,setMode]=useState('person');const [qty,setQty]=useState(2);const [date,setDate]=useState('');const [status,setStatus]=useState('');const seasons=useMemo(()=>{try{return JSON.parse(seasonPrices||'[]') as Season[]}catch{return[]}},[seasonPrices]);const season=seasons.find(s=>date&&date>=s.start&&date<=s.end);const activePerson=season?.personPrice??personPrice;const activeGroup=season?.groupPrice??groupPrice;const total=useMemo(()=>mode==='group'&&activeGroup?activeGroup:activePerson*qty,[mode,activeGroup,activePerson,qty]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Procesando…');const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const response=await fetch('/api/bookings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,tourSlug,tourName,quantity:qty,priceMode:mode,total})});const data=await response.json() as {message?:string;error?:string;reference?:string};setStatus(response.ok?`${data.message} Referencia: ${data.reference}`:(data.error??'No pudimos guardar la reserva.'));}
return <aside className="booking-card" id="reservar"><p className="eyebrow"><span/> Reserva segura</p><h2>Book now</h2><div className="price-line"><strong>${mode==='group'&&activeGroup?activeGroup:activePerson}</strong><span> / {mode==='group'?'grupo':'persona'}</span></div>{groupPrice&&<div className="mode-tabs"><button type="button" className={mode==='person'?'active':''} onClick={()=>setMode('person')}>Por persona</button><button type="button" className={mode==='group'?'active':''} onClick={()=>setMode('group')}>Por grupo</button></div>}<form onSubmit={submit}><label>Fecha<input name="tourDate" type="date" min={new Date().toISOString().split('T')[0]} required value={date} onChange={e=>setDate(e.target.value)}/></label>{season&&<p className="season-note">✦ Aplica {season.name}</p>}<label>Cantidad de personas<div className="stepper"><button type="button" onClick={()=>setQty(Math.max(1,qty-1))}>−</button><b>{qty}</b><button type="button" onClick={()=>setQty(qty+1)}>+</button></div></label><div className="form-row"><label>Nombre<input name="customerName" required placeholder="Tu nombre"/></label><label>Teléfono<input name="phone" required placeholder="+507 ..."/></label></div><label>Correo<input name="email" type="email" required placeholder="nombre@correo.com"/></label><div className="booking-total"><span>Total estimado</span><strong>${total.toFixed(2)}</strong></div><button className="button button--primary full" type="submit">Solicitar reserva <b>→</b></button><small>Sin cobro en línea. Te confirmaremos disponibilidad y el pago será en efectivo.</small>{status&&<p className="form-status" role="status">{status}</p>}</form></aside>}
