'use client';
import { FormEvent,useMemo,useState } from 'react';
type Season={name:string;start:string;end:string;personPrice?:number;groupPrice?:number};
export function BookingWidget({tourSlug,tourName,personPrice,groupPrice,priceType,seasonPrices,minPeople=1}:{tourSlug:string;tourName:string;personPrice:number;groupPrice?:number;priceType:'person'|'group';seasonPrices?:string;minPeople?:number}){const mode=priceType;const [qty,setQty]=useState(Math.max(1,minPeople));const [date,setDate]=useState('');const [status,setStatus]=useState('');const seasons=useMemo(()=>{try{return JSON.parse(seasonPrices||'[]') as Season[]}catch{return[]}},[seasonPrices]);const season=seasons.find(s=>date&&date>=s.start&&date<=s.end);const activePerson=season?.personPrice??personPrice;const activeGroup=season?.groupPrice??groupPrice??personPrice;const total=useMemo(()=>mode==='group'?activeGroup:activePerson*qty,[mode,activeGroup,activePerson,qty]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Procesando…');const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());const response=await fetch('/api/bookings',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,tourSlug,tourName,quantity:qty,priceMode:mode,total})});const data=await response.json() as {message?:string;error?:string;reference?:string};setStatus(response.ok?`${data.message} Referencia: ${data.reference}`:(data.error??'No pudimos guardar la reserva.'));}
return <aside className="booking-card" id="reservar"><p className="eyebrow"><span/> Reserva segura</p><h2>Book now</h2><div className="price-line"><strong>${(mode==='group'?activeGroup:activePerson).toFixed(2)}</strong><span> / {mode==='group'?'grupo':'persona'}</span></div><div className={`booking-mode booking-mode--${mode}`}>Tarifa fija por {mode==='group'?'grupo':'persona'}</div><form onSubmit={submit}><label>Fecha<div className="date-field"><span aria-hidden="true">▣</span><input name="tourDate" type="date" min={new Date().toISOString().split('T')[0]} required value={date} onChange={e=>setDate(e.target.value)}/></div></label>{season&&<p className="season-note">✦ Aplica {season.name}</p>}<label>Cantidad de personas<div className="stepper"><button type="button" aria-label="Restar persona" onClick={()=>setQty(Math.max(minPeople,qty-1))}>−</button><b>{qty}</b><button type="button" aria-label="Agregar persona" onClick={()=>setQty(qty+1)}>+</button></div></label><div className="form-row"><label>Nombre<input name="customerName" required placeholder="Tu nombre"/></label><label>Teléfono<input name="phone" required placeholder="+507 ..."/></label></div><label>Correo<input name="email" type="email" required placeholder="nombre@correo.com"/></label><div className="booking-total"><span>Total estimado</span><strong>${total.toFixed(2)}</strong></div><button className="button button--primary full" type="submit">Solicitar reserva <b>→</b></button><small>Sin cobro en línea. Te confirmaremos disponibilidad y el pago será en efectivo.</small>{status&&<p className="form-status" role="status">{status}</p>}</form></aside>}
