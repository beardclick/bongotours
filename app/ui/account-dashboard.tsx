'use client';
import { FormEvent, useState } from 'react';
import { SiteLink as Link } from './site-link';
type Props={user:{displayName:string;email:string;fullName:string};profile:Record<string,unknown>;bookings:Array<Record<string,unknown>>;signOutPath:string};
const labels:Record<string,string>={pending:'Pendiente',confirmed:'Confirmado',completed:'Completado',cancelled:'Cancelado',cancellation_requested:'Cancelación solicitada'};
export function AccountDashboard({user,profile,bookings:initialBookings,signOutPath}:Props){const [tab,setTab]=useState<'orders'|'profile'|'settings'|'payments'>('orders');const [message,setMessage]=useState('');const [bookings,setBookings]=useState(initialBookings);async function saveProfile(event:FormEvent<HTMLFormElement>){event.preventDefault();setMessage('Guardando…');const data=Object.fromEntries(new FormData(event.currentTarget).entries()) as Record<string,unknown>;data.marketingOptIn=data.marketingOptIn==='on';const response=await fetch('/api/profile',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(data)});setMessage(response.ok?'Perfil actualizado correctamente.':'No se pudo actualizar el perfil.');}async function requestCancel(reference:string){if(!confirm('¿Solicitar la cancelación de este pedido? El equipo revisará la política aplicable.'))return;const response=await fetch('/api/bookings',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action:'request_cancel',reference})});if(response.ok){setBookings(old=>old.map(row=>String(row.reference)===reference?{...row,status:'cancellation_requested'}:row));setMessage('Solicitud enviada. Te contactaremos para confirmarla.');}}
return <section className="account-shell shell"><aside className="account-nav"><div className="user-avatar">{user.displayName.slice(0,2).toUpperCase()}</div><h1>Hola, {user.fullName.split(' ')[0]}</h1><p>{user.email}</p><nav><button className={tab==='orders'?'active':''} onClick={()=>setTab('orders')}>▤ Mis pedidos</button><button className={tab==='profile'?'active':''} onClick={()=>setTab('profile')}>◎ Editar perfil</button><button className={tab==='payments'?'active':''} onClick={()=>setTab('payments')}>◇ Métodos de pago</button><button className={tab==='settings'?'active':''} onClick={()=>setTab('settings')}>⚙ Ajustes</button><a href={signOutPath}>Cerrar sesión</a></nav></aside><div className="account-content">
{tab==='orders'&&<><div className="section-heading"><div><p className="eyebrow"><span/> Tu historial</p><h2>Mis pedidos</h2></div><Link className="button button--primary" href="/tours">Nueva reserva</Link></div>{bookings.length===0?<div className="empty-state"><span>△</span><h3>Aún no tienes pedidos</h3><p>Tu próxima historia puede empezar en el Volcán Barú, Boquete o una isla del Pacífico.</p><Link className="button" href="/tours">Ver experiencias</Link></div>:<><div className="booking-list">{bookings.map(row=><article key={String(row.reference)}><div><small>{String(row.reference)}</small><h3>{String(row.tour_slug).replaceAll('-',' ')}</h3></div><div><small>Fecha</small><b>{String(row.tour_date)}</b></div><div><small>Cantidad</small><b>{String(row.quantity)}</b></div><div><small>Total</small><b>${Number(row.total).toFixed(2)}</b></div><span className={`status-pill status-${String(row.status)}`}>{labels[String(row.status)]??String(row.status)}</span>{['pending','confirmed'].includes(String(row.status))&&<button className="order-cancel" onClick={()=>requestCancel(String(row.reference))}>Solicitar cancelación</button>}</article>)}</div><p className="account-message">{message}</p></>}</>}
{tab==='profile'&&<section className="account-panel"><p className="eyebrow"><span/> Información personal</p><h2>Editar perfil</h2><form className="admin-form" onSubmit={saveProfile}><div className="form-row"><label>Nombre completo<input name="fullName" required defaultValue={String(profile.full_name??user.fullName)} /></label><label>Correo electrónico<input value={user.email} disabled /></label></div><div className="form-row"><label>Teléfono<input name="phone" defaultValue={String(profile.phone??'')} /></label><label>Ciudad<input name="city" defaultValue={String(profile.city??'')} /></label></div><div className="form-row"><label>País<input name="country" defaultValue={String(profile.country??'Panamá')} /></label><label>Contacto de emergencia<input name="emergencyContact" defaultValue={String(profile.emergency_contact??'')} /></label></div><div className="admin-form__actions"><span>{message}</span><button className="button button--primary">Guardar cambios</button></div></form></section>}
{tab==='payments'&&<section className="account-panel"><p className="eyebrow"><span/> Pagos seguros</p><h2>Métodos de pago</h2><article className="payment-method"><span>＄</span><div><h3>Efectivo</h3><p>Método activo para las reservas actuales. El equipo confirma los detalles contigo.</p></div><b>Predeterminado</b></article><article className="payment-coming"><span>◇</span><div><h3>Tarjetas con Paguelo Fácil</h3><p>Al activar la pasarela, podrás agregar y administrar tarjetas mediante tokenización segura. Bongo no almacenará números de tarjeta.</p></div><small>Próximamente</small></article></section>}
{tab==='settings'&&<section className="account-panel"><p className="eyebrow"><span/> Preferencias</p><h2>Ajustes</h2><form className="admin-form" onSubmit={saveProfile}><input type="hidden" name="fullName" value={String(profile.full_name??user.fullName)}/><input type="hidden" name="phone" value={String(profile.phone??'')}/><input type="hidden" name="country" value={String(profile.country??'Panamá')}/><input type="hidden" name="city" value={String(profile.city??'')}/><input type="hidden" name="emergencyContact" value={String(profile.emergency_contact??'')}/><label className="setting-row"><span><b>Novedades y ofertas</b><small>Recibe novedades de tours y temporadas por correo.</small></span><input type="checkbox" name="marketingOptIn" defaultChecked={Boolean(profile.marketing_opt_in)}/></label><div className="admin-form__actions"><span>{message}</span><button className="button button--primary">Guardar ajustes</button></div></form></section>}</div></section>}
