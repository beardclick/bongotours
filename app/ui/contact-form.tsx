'use client';
import { FormEvent,useState } from 'react';
export function ContactForm(){const [status,setStatus]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setStatus('');const data=Object.fromEntries(new FormData(e.currentTarget).entries());const res=await fetch('/api/forms',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...data,page:window.location.href})});const json=await res.json() as {message?:string;error?:string};setStatus(res.ok?json.message||'Mensaje enviado.':json.error||'No se pudo enviar.');setBusy(false);if(res.ok)e.currentTarget.reset();}return <form className="contact-form" onSubmit={submit}><h2>Cuéntanos tu idea</h2><div className="form-row"><label>Nombre<input name="name" required placeholder="Tu nombre"/></label><label>Correo<input name="email" type="email" required placeholder="nombre@correo.com"/></label></div><label>¿Qué te interesa?<select name="interest"><option>Un tour</option><option>Hospedaje</option><option>Shuttle</option><option>Servicio de drones</option><option>Grupo privado</option></select></label><label>Mensaje<textarea name="message" rows={6} required placeholder="Fechas, cantidad de personas y cualquier detalle…"/></label><button className="button button--primary full" type="submit" disabled={busy}>{busy?'Enviando…':'Enviar consulta'}</button>{status&&<p className="form-status" role="status">{status}</p>}</form>}
