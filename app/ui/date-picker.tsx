'use client';
import { useMemo,useState } from 'react';
import { FaCalendarDays,FaChevronLeft,FaChevronRight } from 'react-icons/fa6';
const iso=(date:Date)=>`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;
export function DatePicker({value,onChange}:{value:string;onChange:(value:string)=>void}){const today=useMemo(()=>{const date=new Date();date.setHours(0,0,0,0);return date},[]);const selected=value?new Date(`${value}T12:00:00`):null;const [open,setOpen]=useState(false);const [view,setView]=useState(()=>new Date(today.getFullYear(),today.getMonth(),1));const days=useMemo(()=>{const start=new Date(view.getFullYear(),view.getMonth(),1);start.setDate(start.getDate()-((start.getDay()+6)%7));return Array.from({length:42},(_,index)=>{const date=new Date(start);date.setDate(start.getDate()+index);return date})},[view]);const label=selected?selected.toLocaleDateString('es-PA',{weekday:'short',day:'numeric',month:'long',year:'numeric'}):'Selecciona una fecha';return <div className={`modern-date ${open?'is-open':''}`}><input type="hidden" name="tourDate" value={value}/><button type="button" className="modern-date__trigger" aria-expanded={open} onClick={()=>setOpen(!open)}><FaCalendarDays/><span>{label}</span></button>{open&&<div className="modern-date__popover"><header><button type="button" aria-label="Mes anterior" onClick={()=>setView(new Date(view.getFullYear(),view.getMonth()-1,1))}><FaChevronLeft/></button><strong>{view.toLocaleDateString('es-PA',{month:'long',year:'numeric'})}</strong><button type="button" aria-label="Mes siguiente" onClick={()=>setView(new Date(view.getFullYear(),view.getMonth()+1,1))}><FaChevronRight/></button></header><div className="modern-date__week">{['L','M','M','J','V','S','D'].map((day,index)=><span key={`${day}-${index}`}>{day}</span>)}</div><div className="modern-date__days">{days.map(day=>{const disabled=day<today;const outside=day.getMonth()!==view.getMonth();const key=iso(day);return <button type="button" key={key} disabled={disabled} className={`${outside?'outside ':''}${value===key?'selected':''}`} onClick={()=>{onChange(key);setOpen(false)}}>{day.getDate()}</button>})}</div><footer><span>Fechas pasadas no disponibles</span><button type="button" onClick={()=>{setView(new Date(today.getFullYear(),today.getMonth(),1));onChange(iso(today));setOpen(false)}}>Hoy</button></footer></div>}</div>}
