'use client';
import { FormEvent,useState } from 'react';
export function ReviewForm({tourSlug}:{tourSlug:string}){const [status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const body={...Object.fromEntries(new FormData(form).entries()),tourSlug};const res=await fetch('/api/reviews',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await res.json() as {message?:string;error?:string};setStatus(res.ok?(data.message??'¡Gracias!'):(data.error??'No se pudo enviar.'));if(res.ok)form.reset();}return <form className="simple-form" onSubmit={submit}><div className="form-row"><label>Nombre<input name="name" required/></label><label>Calificación<select name="rating"><option value="5">5 — Excelente</option><option value="4">4 — Muy bueno</option><option value="3">3 — Bueno</option></select></label></div><label>Tu reseña<textarea name="comment" rows={4} required/></label><button className="button" type="submit">Enviar reseña</button>{status&&<p className="form-status" role="status">{status}</p>}</form>}
