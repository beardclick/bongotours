import { NextResponse } from 'next/server';
import { getChatGPTUser } from '../../chatgpt-auth';
import { verifyTurnstile } from '../../lib/turnstile';
import { ensureDatabase } from '../../../db/runtime';
export async function POST(request:Request){const body=await request.json() as Record<string,unknown>;if(!await verifyTurnstile(request,body.turnstileToken,'review'))return NextResponse.json({error:'Completa la verificación de seguridad.'},{status:400});if(!body.tourSlug||!body.name||!body.comment)return NextResponse.json({error:'Completa la reseña.'},{status:400});const db=await ensureDatabase();const user=await getChatGPTUser();await db.prepare(`INSERT INTO reviews (tour_slug,user_id,name,rating,comment,approved,created_at) VALUES (?,?,?,?,?,0,?)`).bind(body.tourSlug,user?.userId??null,body.name,Math.min(5,Math.max(1,Number(body.rating||5))),body.comment,new Date().toISOString()).run();return NextResponse.json({ok:true,message:'Gracias. Tu reseña será publicada después de revisarla.'});}
