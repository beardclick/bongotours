import { NextResponse } from 'next/server';import { cookies } from 'next/headers';import { authCookies,supabaseAuthRequest } from '../../../chatgpt-auth';import { cleanReturnTo,clearSession } from '../helpers';
export async function GET(request:Request){const url=new URL(request.url);const returnTo=cleanReturnTo(url.searchParams.get('return_to'));const access=(await cookies()).get(authCookies.access)?.value;if(access)await supabaseAuthRequest('/logout',{method:'POST',headers:{Authorization:`Bearer ${access}`}}).catch(()=>null);return clearSession(NextResponse.redirect(new URL(returnTo,url.origin)))}
