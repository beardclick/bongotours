import type { Metadata } from 'next';import { Header } from '../ui/header';import { Footer } from '../ui/footer';import { WhatsApp } from '../ui/whatsapp';import { getLodgings } from '../lib/content';
export const dynamic='force-dynamic';export const metadata:Metadata={title:'Hospedajes y cabañas en Chiriquí',description:'Hospedajes seleccionados en Boquete y Chiriquí con tarifas por persona o grupo.'};
const defaults=[{name:'Cabaña Bosque Alto',location:'Alto Boquete',image:'https://images.unsplash.com/photo-1449158743715-0a90ebb6d2d8?auto=format&fit=crop&w=1200&q=85',person:42,group:210},{name:'Casa del Sendero',location:'Bajo Mono, Boquete',image:'https://images.unsplash.com/photo-1520250497591-112f2f40a3f4?auto=format&fit=crop&w=1200&q=85',person:55,group:260},{name:'Refugio del Pacífico',location:'Boca Chica',image:'https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=1200&q=85',person:68,group:340}];
export default async function Hospedajes(){const rows=await getLodgings() as Array<{name:string;location:string;description:string;person_price:number;group_price:number;image:string|null}>;const stays=[...rows.map(r=>({name:r.name,location:r.location,image:r.image||'/images/bongo-hero.png',person:r.person_price||0,group:r.group_price||0,description:r.description})),...defaults.map(r=>({...r,description:'Un espacio cómodo, tranquilo y bien ubicado para comenzar temprano tu próxima experiencia.'}))];return <main><Header/><section className="page-hero page-hero--green"><div className="shell"><p className="eyebrow eyebrow--light"><span/> Descansa bien</p><h1>Hospedajes con alma de aventura.</h1><p>Opciones por persona o para tu grupo completo.</p></div></section><section className="section shell"><div className="stay-grid">{stays.map((s,index)=><article key={`${s.name}-${index}`}><img src={s.image} alt={s.name}/><div><small>⌖ {s.location}</small><h2>{s.name}</h2><p>{s.description}</p><div className="stay-price"><span><b>${s.person}</b> / persona</span><span><b>${s.group}</b> / grupo</span></div><a className="button" href={`https://wa.me/50764467276?text=${encodeURIComponent(`Hola, deseo reservar ${s.name}`)}`} target="_blank">Consultar disponibilidad</a></div></article>)}</div></section><Footer/><WhatsApp/></main>}
