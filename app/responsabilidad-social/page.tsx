import { Header } from '../ui/header';import { Footer } from '../ui/footer';
import { getPageConfig } from '../lib/site-config';
const fallbackContent='<h2>Local primero</h2><p>Priorizamos guías, transportistas, fincas y pequeños proveedores panameños. Así, una mayor parte de cada reserva permanece en el destino.</p><h2>Grupos pequeños</h2><p>Reducen el impacto, mejoran la seguridad y permiten una experiencia genuinamente cercana.</p><h2>Respeto por la naturaleza</h2><p>Aplicamos principios de no dejar rastro, evitamos plásticos de un solo uso cuando es posible y seguimos las normas de cada área protegida.</p>';
export const dynamic='force-dynamic';
export default async function Responsabilidad(){const hero=await getPageConfig('responsabilidad-social');return <main><Header/><section className="page-hero page-hero--green"><div className="shell"><p className="eyebrow eyebrow--light"><span/> {hero.eyebrow}</p><h1>{hero.title}</h1><p>{hero.description}</p></div></section><article className="section shell prose"><div dangerouslySetInnerHTML={{__html:hero.content||fallbackContent}}/></article><Footer/></main>}
